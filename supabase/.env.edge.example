# Copie para supabase/.env.edge (nao commitar) e use com:
# supabase functions serve asaas-webhook --env-file supabase/.env.edge
#
# Em producao, defina o secret no projeto:
# supabase secrets set ASAAS_WEBHOOK_TOKEN=<mesmo_token_do_painel_asaas>

ASAAS_WEBHOOK_TOKEN=
